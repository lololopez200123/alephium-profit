import { Box } from '@mui/material';

function Notfications() {
  return (
    <Box
      sx={{
        display: 'flex',
        paddingBlock: '20%',
        flexDirection: 'column',
        width: '100%',
        height: '100%',
        paddingX: '1rem',
        overflowX: 'hidden',
        overflowY: 'auto',
      }}
    ></Box>
  );
}

export default Notfications;
